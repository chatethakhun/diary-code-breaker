<p id="notice"><%= notice %></p>

<h1>Content Types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @content_types.each do |content_type| %>
      <tr>
        <td><%= content_type.name %></td>
        <td><%= link_to 'Show', content_type %></td>
        <td><%= link_to 'Edit', edit_content_type_path(content_type) %></td>
        <td><%= link_to 'Destroy', content_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'Create content', new_content_type_content_path(content_type) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Content Type', new_content_type_path %>
